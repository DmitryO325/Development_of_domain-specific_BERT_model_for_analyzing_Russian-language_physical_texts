# Аудит источников корпуса v1

- **Версия:** 0.6.1
- **Состояние:** рабочий артефакт этапа 2
- **Дата проверки:** 7 сентября 2026 года
- **Последнее уточнение:** 21 сентября 2026 года — завершена выгрузка метаданных ОИЯИ и выполнено локальное сопоставление PDF
- **Область:** все доступные русскоязычные журнальные статьи по физике,
  без фильтра по году и верхнего предела по числу статей

Этот документ объединяет реестр приоритетных источников и аудит разрешённых
операций. Каталог всех найденных ресурсов остаётся в
[`RESOURCES.md`](RESOURCES.md), а требования к корпусу — в
[`cards/CORPUS_V1.md`](cards/CORPUS_V1.md).

Документ фиксирует осознанное рабочее допущение проекта, а не даёт юридическое
заключение. Правомерный доступ к полному тексту и отсутствие явного применимого
запрета считаются достаточным основанием для закрытого некоммерческого хранения,
оценки и ML-обучения. Отсутствие в условиях отдельной фразы про ML-обучение само
по себе не создаёт статус `unknown` и не блокирует внутренний эксперимент.

Это допущение не разрешает обход авторизации или платного доступа, нарушение
ограничений на способ и масштаб получения, передачу полных текстов, публикацию
производного корпуса или контрольной точки. Явный запрет и условия применимого
договора имеют приоритет. Возможность массового автоматического получения и
возможность публичного выпуска проверяются отдельно от внутреннего использования.

## 1. Состояния разрешений

Для каждой операции используется одно из пяти состояний:

- `allowed` — операция прямо разрешена публичными условиями или отдельным
  письменным разрешением;
- `conditional` — операция допускается только при перечисленных условиях; для
  внутреннего исследования сюда относится рабочее допущение проекта;
- `permission_required` — источник прямо требует отдельного разрешения;
- `prohibited` — операция прямо запрещена и не допускает согласования в рамках
  найденных условий;
- `unknown` — не установлен правомерный доступ, не прочитаны применимые условия
  либо не разрешено существенное противоречие между ними.

`permission_required` и `prohibited` блокируют соответствующую операцию до
получения разрешения или изменения источника. `unknown` блокирует операцию,
только если неясность относится к необходимому основанию доступа, применимому
договору, способу получения или явному ограничению. Одно лишь отсутствие
упоминания ML-обучения не является такой неясностью. Оценка и обучение всё равно
невозможны, если материал нельзя правомерно получить и хранить.

Для `conditional` по рабочему допущению используются одинаковые условия:

- доступ получен правомерно;
- использование закрытое и некоммерческое;
- в применимых условиях нет явного несовместимого запрета;
- авторизация, платный доступ и технические ограничения не обходятся;
- полный текст и производный корпус не передаются третьим лицам.

Основание такого решения записывается как
`basis_type=lawful_access_research_assumption`. Канонические значения поля:
`explicit_license`, `written_permission`, `contract`, `statutory_exception`,
`lawful_access_research_assumption` и `unknown`.

Канонические поля реестра прав:

- `acquisition_allowed` — получение материалов выбранным способом и в
  зафиксированном масштабе;
- `storage_allowed` — локальное хранение исходных материалов;
- `evaluation_allowed` — машинная оценка моделей на материале;
- `ml_training_allowed` — предварительное обучение или дообучение модели;
- `redistribution_allowed` — передача исходных или очищенных полных текстов;
- `derivatives_release_allowed` — публикация производных наборов и фрагментов;
- `checkpoint_release_allowed` — публикация контрольной точки модели.

Публичная доступность для чтения хранится отдельно в поле
`public_read_access` со значениями `yes|conditional|paid|unknown`. Технические
правила обхода сайта хранятся отдельно от правового основания. Для получения
дополнительно фиксируются `acquisition_method` со значениями
`manual_download|api|crawler|platform_export|other`, `acquisition_scope` со
значениями `single|sample|bulk`, ожидаемый и фактический объём, частота запросов и применимые
правила `robots.txt`: ручная загрузка, официальный API и массовый обход сайта не
считаются одной операцией.

`derivatives_release_allowed` всегда сопровождается областью действия:
агрегированные метрики, библиографические идентификаторы, короткие фрагменты и
производный набор текстов проверяются отдельно. Разрешение одного типа
производного артефакта не переносится на остальные.

## 2. Охват источников

Идентификаторы `S01`–`S06` ниже обозначают `source_group_id`: отдельный журнал
или семейство журналов на общей платформе. Перед сбором каждый конкретный
журнал и языковая версия получают собственный `source_id`; портал не считается
одним журналом.

Для каждого допущенного `source_group_id` планируется получить все пригодные русскоязычные
физические статьи, доступные к моменту `corpus_cutoff_at` и прошедшие критерии допуска.
Источники `S01_UFN`, `S02_JETP_LETTERS`, `S03_SCIENCEJOURNALS`, `S04_JETP`, `S05_IOFFE`
и `S06_QUANTUM_ELECTRONICS` не имеют верхних квот.

Для готовности H1 нужны не менее четырёх `source_group_id` и 2 000 работ суммарно. Это
минимальный порог, а не сигнал остановить сбор. Фактические доли источников и поддоменов
описываются в паспорте, учитываются при разбиении и взвешивании, но статьи не отбрасываются
только ради искусственной квоты.

Полнота охвата проверяется отдельно для каждого источника. До фиксации версии нужно указать
границы охвата архива и свести количества найденных, полученных, допущенных, исключённых
и неразрешённых кандидатов. Если есть неразрешённые пропуски, версия прямо обозначается как
неполная по этому источнику.

Для H2 квоты пока не назначены. Сначала аудит 200 статей, включая 150 целевых
кандидатов, должен показать, где доступны надёжные коды ГРНТИ или достаточные
основания для экспертной разметки. В каждом итоговом классе нужны минимум два
`source_id` конкретных журналов, а целевая доля одного `source_id` не должна
превышать 60%.

## 3. Приоритетные источники

### S01_UFN — «Успехи физических наук»

- **Приоритет:** основной кандидат.
- **Состояние:** `collection_limited`; значительное массовое получение требует
  разрешения, внутреннее исследование правомерно полученных копий допускается
  условно.
- **Роль:** широкий физический корпус H1; обзоры и исследовательские статьи;
  кандидат H2 только после аудита меток.
- **Охват:** весь доступный и допущенный русскоязычный архив.
- **Публичный доступ:** сайт допускает индивидуальное некоммерческое
  использование материалов преподавателями, студентами и исследователями.
- **`public_read_access`:** `conditional`.
- **Планируемый способ получения:** сначала метаданные и ручная выборка; массовое
  получение PDF — только после письменного разрешения.
- **Режимы реестра:** `manual_download|sample` — `conditional` для правомерно
  доступной исследовательской выборки; `crawler|sample` — `unknown` до
  отдельного разъяснения об автоматическом получении; `crawler|bulk` —
  `permission_required`.
- **Технические ограничения:** соблюдать
  [`robots.txt`](https://ufn.ru/robots.txt); закрытые пути поиска, учётной записи
  и служебные пути не обходить.
- **Условия:** [пользовательское соглашение](https://ufn.ru/ru/copyright.html).
- **Контакт:** `ufn@ufn.ru`.
- **Проверено:** 30 августа 2026 года; сохранено доказательство
  [`evidence/UFN_RIGHTS_2026-08-30.md`](evidence/UFN_RIGHTS_2026-08-30.md).

Разрешения для планируемого массового корпуса:

- `acquisition_allowed`: `permission_required`;
- `storage_allowed`: `conditional` для правомерно полученных копий; масштаб
  хранилища не может обходить ограничение на значительное массовое копирование;
- `evaluation_allowed`: `conditional`;
- `ml_training_allowed`: `conditional`;
- `redistribution_allowed`: `permission_required`;
- `derivatives_release_allowed`: `unknown`;
- `checkpoint_release_allowed`: `unknown`.

`basis_type=lawful_access_research_assumption` для хранения, оценки и обучения;
условия — закрытое некоммерческое использование без передачи полных текстов.

**Основание решения:** соглашение разрешает индивидуальное исследовательское
использование, но запрещает значительное массовое копирование и вторичное
распространение без письменного разрешения. Отсутствие отдельного упоминания
машинной оценки и ML-обучения не препятствует их закрытому некоммерческому
применению к правомерно полученным копиям. Это не распространяется на массовое
получение и публичный выпуск.

**Отправлено 31.08.2026:**
[`письмо в редакцию УФН`](evidence/UFN_PERMISSION_REQUEST_2026-08-31.md) с
запросом на пилот из 10 статей, последующее использование всего доступного
архива и публикацию обученной модели. Факт отправки подтверждён автором проекта
и сохранён в
[`отдельной записи`](evidence/UFN_PERMISSION_REQUEST_SENT_2026-08-31.md).
SHA-256 текста письма:
`a6ab0a951e75436c3cfd29aa0d672c1696d697c59f10b60f2cd8f89c77906734`.
SHA-256 записи об отправке:
`1d66a7daa34af718059afd00c380870ba3f14575e71ab7f4e73a0213600639f5`.

**Ручной инженерный пилот 31.08.2026:** до получения ответа вручную загружены и
зарегистрированы 10 PDF, доступных на официальном сайте УФН. Для всех файлов
технически извлечён текст, однако предварительная ручная проверка выявила
ошибки OCR, и качественный допуск не пройден. Все работы относятся к 2026 году; сам год больше
не исключает их из corpus v1, но пилот не допущен в обучение до подтверждения прав и качества. Этот
ограниченный пилот не меняет статус массового
автоматического получения, публикации корпуса или контрольной точки и не
считается разрешением на использование всего архива. Подробности:
[`UFN_OCR_PILOT.md`](UFN_OCR_PILOT.md).

**Следующее действие:** дождаться ответа редакции, сохранить его с датой,
отправителем и контрольной суммой, затем оформить новые записи `rights-v1` без
изменения прежней истории.

**Запасной вариант:** `F01_MATHNET` для метаданных и отдельных доступных копий;
`F02_OPEN_LICENSE_ARTICLES` для замены работ, права на которые не подтверждены.

### S02_JETP_LETTERS — «Письма в ЖЭТФ»

- **Приоритет:** основной кандидат.
- **Состояние:** `eligible_conditional` для закрытого некоммерческого
  исследования русских материалов; способ массового автоматического получения
  требует отдельной технической фиксации.
- **Роль:** короткие исследовательские статьи широкого физического профиля;
  кандидат H1 и H2.
- **Охват:** весь доступный и допущенный русскоязычный архив.
- **Публичный доступ:** официальный сайт сообщает о полном архиве русской
  версии; доступ к английским статьям с тома 63 официальный сайт прямо называет
  платным.
- **`public_read_access`:** `yes` для русского архива, `paid` для английских
  статей с тома 63.
- **Планируемый способ получения:** русские метаданные и PDF с официального
  архива с консервативным ограничением частоты запросов; платную английскую
  версию не использовать и авторизацию не обходить.
- **Плановый режим реестра:** `crawler|bulk`; статус `conditional` только при
  зафиксированных путях, частоте запросов и отсутствии технического запрета.
- **Технические ограничения:** действующий `robots.txt` на проверенном HTTP-узле
  не найден; это не считается запретом, но перед массовым запуском нужно
  зафиксировать доступные пути, частоту запросов и реакцию на ограничения сайта.
- **Условия и описание архива:**
  [официальная страница журнала](http://jetpletters.ru/en/jetpl.shtml),
  [информация для авторов](http://jetpletters.ru/en/info.shtml).
- **Контакт:** `letters@kapitza.ras.ru`.
- **Проверено:** 19 августа 2026 года.

Разрешения:

- `acquisition_allowed`: `conditional` для публичных русских материалов при
  ограниченной частоте запросов и отсутствии явного технического запрета;
- `storage_allowed`: `conditional`;
- `evaluation_allowed`: `conditional`;
- `ml_training_allowed`: `conditional`;
- `redistribution_allowed`: `unknown`;
- `derivatives_release_allowed`: `unknown`;
- `checkpoint_release_allowed`: `unknown`.

`basis_type=lawful_access_research_assumption` для получения, хранения, оценки и
обучения; условия — только публичный русский архив, закрытое некоммерческое
использование, отсутствие обхода платного доступа и передача текстов исключена.

**Основание решения:** русский архив доступен правомерно, а явного применимого
запрета на закрытый вычислительный анализ не найдено. Отсутствие публичной
лицензии повторного использования сохраняет неопределённость для передачи
текстов и публичного выпуска, но не блокирует внутреннее исследование.

**Следующее действие:** дождаться ответа редакции; до массового обхода
зафиксировать разрешённый маршрут, частоту запросов и условия публичного выпуска.

**Запасной вариант:** `F01_MATHNET` для библиографических записей и правомерно
доступных копий; `F02_OPEN_LICENSE_ARTICLES` для замены полного текста.

### S03_SCIENCEJOURNALS — портал научных журналов РАН

- **Приоритет:** основной кандидат после разбиения по конкретным журналам.
- **Состояние:** `terms_review_pending` для полного текста; отдельная фраза про
  ML-обучение не требуется, но применимый договор доступа ещё не прочитан.
- **Роль:** расширение источников и физических подразделов H1/H2.
- **Охват:** все доступные и допущенные русскоязычные физические статьи; каждый
  конкретный журнал сохраняет отдельный `source_id`.
- **Публичный доступ:** метаданные доступны публично; доступ к полным текстам
  научных журналов РАН предоставляется по контрактам РАН и возможен только
  после авторизации.
- **`public_read_access`:** `yes` для публичных метаданных, `conditional` для
  полных текстов.
- **Планируемый способ получения:** публичные метаданные; полные тексты — только
  через действительный авторизованный доступ и после проверки применимого
  договора на отсутствие явного несовместимого ограничения.
- **Плановый режим реестра:** официальный `platform_export|bulk`, если он
  предусмотрен договором; `crawler|bulk` остаётся `unknown` до проверки условий.
- **Технические ограничения:** `robots.txt` при проверке не открылся; правила
  автоматического доступа неизвестны, авторизацию и ограничения договора не
  обходить.
- **Условия:** [пользовательское соглашение](https://www.sciencejournals.ru/pub/oferta.pdf),
  [главная страница](https://www.sciencejournals.ru/).
- **Состояние доказательства:** официальный PDF соглашения найден по ссылке, но
  при проверке не загрузился; его содержание не считается прочитанным.
- **Контакт:** `info@sciencejournals.ru`.
- **Проверено:** 19 августа 2026 года.

Разрешения для полного текста:

- `acquisition_allowed`: `unknown`;
- `storage_allowed`: `unknown`;
- `evaluation_allowed`: `unknown`;
- `ml_training_allowed`: `unknown`;
- `redistribution_allowed`: `unknown`;
- `derivatives_release_allowed`: `unknown`;
- `checkpoint_release_allowed`: `unknown`.

Текущее `basis_type=unknown`: применимый договор ещё не прочитан. Ожидаемый
переход после совместимой проверки — отдельная запись `basis_type=contract` для
доступа и отдельные `conditional`-записи
`basis_type=lawful_access_research_assumption` для закрытых хранения, оценки и
обучения. До этого перехода операции полного текста заблокированы.

**Основание решения:** действительный договорный доступ может быть основанием
для внутреннего исследования, но не разрешает обход авторизации и сам по себе
не определяет допустимый масштаб автоматического получения. Для конкретного
договора и журнала достаточно проверить отсутствие явного несовместимого
ограничения; отдельное положительное упоминание ML-обучения не требуется.

**Следующее действие:** дождаться ответа администрации портала и получить текст
применимого договора доступа; затем выделить физические журналы и проверить
ограничения автоматического получения, локального хранения, вычислительного
анализа и публичного выпуска для каждого издательского набора.

**Запасной вариант:** `F02_OPEN_LICENSE_ARTICLES`; для меток H2 —
`F03_BIBLIOGRAPHIC_METADATA`, если право на использование записей подтверждено.

### S04_JETP — «Журнал экспериментальной и теоретической физики»

- **Приоритет:** основной кандидат.
- **Состояние:** `eligible_conditional` для закрытого некоммерческого
  исследования публичного русского архива.
- **Роль:** широкий корпус теоретической и экспериментальной физики для H1;
  кандидат H2.
- **Охват:** весь доступный и допущенный русскоязычный архив.
- **Публичный доступ:** русский архив опубликован на официальном сайте; с 2024
  года статьи на платформе РЦНИ заявлены как открыто доступные.
- **`public_read_access`:** `yes` для публичной части русского архива.
- **Планируемый способ получения:** метаданные и публичные PDF официального
  русского архива с соблюдением правил автоматического доступа.
- **Плановый режим реестра:** `crawler|bulk`, `conditional` при соблюдении
  `robots.txt`, `Crawl-delay: 10` и ограничений путей.
- **Технические ограничения:** соблюдать
  [`robots.txt`](https://www.jetp.ras.ru/robots.txt) и `Crawl-delay: 10`;
  служебные и загрузочные пути не использовать.
- **Условия и права авторов:**
  [информация для авторов](https://www.jetp.ras.ru/cgi-bin/r/index/information-for-authors),
  [русский архив](https://www.jetp.ras.ru/cgi-bin/r/index).
- **Контакт:** `jetp@kapitza.ras.ru`.
- **Проверено:** 19 августа 2026 года.

Разрешения:

- `acquisition_allowed`: `conditional` при соблюдении `robots.txt`,
  `Crawl-delay: 10` и ограничений путей;
- `storage_allowed`: `conditional`;
- `evaluation_allowed`: `conditional`;
- `ml_training_allowed`: `conditional`;
- `redistribution_allowed`: `unknown`;
- `derivatives_release_allowed`: `unknown`;
- `checkpoint_release_allowed`: `unknown`.

`basis_type=lawful_access_research_assumption` для получения, хранения, оценки и
обучения; условия — публичный русский архив, закрытое некоммерческое
использование и соблюдение технических ограничений.

**Основание решения:** публичный русский архив и отсутствие явного применимого
запрета достаточны по рабочему допущению для закрытого некоммерческого анализа и
обучения. На проверенных страницах не найдена общая лицензия повторного
использования, поэтому передача текстов и публичный выпуск остаются отдельными
операциями. Передача прав издателю в авторских условиях не выдаёт автоматически
лицензию на распространение пользователю корпуса.

**Следующее действие:** дождаться ответа редакции; до массового сбора
зафиксировать соблюдение `robots.txt`, допустимую частоту запросов и условия
выпуска контрольной точки.

**Запасной вариант:** `F01_MATHNET` для метаданных и отдельных копий;
`F02_OPEN_LICENSE_ARTICLES` для замены неподтверждённых полных текстов.

### S05_IOFFE — журналы ФТИ им. А. Ф. Иоффе

- **Приоритет:** основной кандидат.
- **Состояние:** `eligible_conditional` для закрытого некоммерческого
  исследования публичных русских материалов.
- **Роль:** твёрдое тело, полупроводники, техническая физика и оптика; H1 и
  отдельные классы H2 с контролем смешения класса и журнала.
- **Охват:** все доступные и допущенные русскоязычные статьи всех подходящих журналов.
- **Публичный доступ:** официальный портал сообщает о свободном доступе к
  полнотекстовым электронным версиям с 1988 года.
- **`public_read_access`:** `yes`.
- **Планируемый способ получения:** метаданные и публичные PDF с соблюдением
  правил автоматического доступа; каждый журнал и языковая версия получают
  отдельный `source_id`.
- **Плановый режим реестра:** `crawler|bulk`, `conditional` при соблюдении
  `robots.txt`, ограничений путей и зафиксированной частоты запросов.
- **Технические ограничения:** соблюдать
  [`robots.txt`](https://journals.ioffe.ru/robots.txt); закрытые AJAX-пути не
  использовать.
- **Условия и архив:** [официальный портал](https://journals.ioffe.ru/),
  [авторский договор](https://journals.ioffe.ru/misc/physica-dogovor.pdf),
  [правила открытого доступа отдельных статей](https://journals.ioffe.ru/journals/rules/14).
- **Контакт:** `journals@ioffe.ru`.
- **Проверено:** 19 августа 2026 года.

Разрешения:

- `acquisition_allowed`: `conditional` при соблюдении `robots.txt`, ограничений
  путей и консервативной частоты запросов;
- `storage_allowed`: `conditional`;
- `evaluation_allowed`: `conditional`;
- `ml_training_allowed`: `conditional`;
- `redistribution_allowed`: `unknown`;
- `derivatives_release_allowed`: `unknown`;
- `checkpoint_release_allowed`: `unknown`.

`basis_type=lawful_access_research_assumption` для получения, хранения, оценки и
обучения; условия — публичные русские материалы, закрытое некоммерческое
использование, соблюдение `robots.txt` и отсутствие передачи текстов.

**Основание решения:** свободный доступ и отсутствие явного применимого запрета
достаточны по рабочему допущению для закрытого некоммерческого анализа и
обучения, но не являются публичной лицензией на распространение. На проверенных
страницах такая лицензия для всего портала не найдена. Если конкретная статья
имеет явные реквизиты Creative Commons, её права на публичный выпуск проверяются
отдельно и не распространяются на остальные статьи.

**Следующее действие:** дождаться ответа издателя; до массового сбора
зафиксировать используемые пути, допустимую частоту запросов, соблюдение
`robots.txt` и условия публичного выпуска.

**Запасной вариант:** `F02_OPEN_LICENSE_ARTICLES` из тех же подразделов;
`F03_BIBLIOGRAPHIC_METADATA` для проверки класса без полного текста.

### S06_QUANTUM_ELECTRONICS — «Квантовая электроника»

- **Приоритет:** основной узкопредметный кандидат.
- **Состояние:** `eligible_conditional` для закрытого некоммерческого
  исследования публичных русских материалов.
- **Роль:** лазерная физика и квантовая электроника; H1 и класс 29.33 H2.
- **Охват:** весь доступный и допущенный русскоязычный архив.
- **Публичный доступ:** на официальном сайте доступны страницы статей и PDF.
- **`public_read_access`:** `yes`.
- **Планируемый способ получения:** метаданные и публичные PDF с соблюдением
  правил автоматического доступа.
- **Плановый режим реестра:** `crawler|bulk`, `conditional` при соблюдении
  `robots.txt`, ограничений путей и зафиксированной частоты запросов.
- **Технические ограничения:** соблюдать
  [`robots.txt`](https://quantum-electronics.ru/robots.txt); служебный раздел
  WordPress не использовать.
- **Условия:** [официальный сайт](https://quantum-electronics.ru/),
  [архив](https://quantum-electronics.ru/archive/),
  [авторский договор](https://quantum-electronics.ru/wp-content/uploads/2020/12/author_agreement.pdf).
- **Контакт:** `ke@lebedev.ru`.
- **Проверено:** 19 августа 2026 года.

Разрешения:

- `acquisition_allowed`: `conditional` при соблюдении `robots.txt`, ограничений
  путей и консервативной частоты запросов;
- `storage_allowed`: `conditional`;
- `evaluation_allowed`: `conditional`;
- `ml_training_allowed`: `conditional`;
- `redistribution_allowed`: `unknown`;
- `derivatives_release_allowed`: `unknown`;
- `checkpoint_release_allowed`: `unknown`.

`basis_type=lawful_access_research_assumption` для получения, хранения, оценки и
обучения; условия — публичные русские материалы, закрытое некоммерческое
использование, соблюдение `robots.txt` и отсутствие передачи текстов.

**Основание решения:** публичный доступ и отсутствие явного применимого запрета
достаточны по рабочему допущению для закрытого некоммерческого анализа и
обучения. Авторский договор не является публичной лицензией на распространение
для третьих лиц. Найденное при выборочной проверке упоминание CC BY 4.0 внутри
одного PDF было ссылкой на внешний материал, а не заявлением о лицензии всей
статьи; право на публичный выпуск нужно подтверждать по явной издательской
отметке каждой работы.

**Следующее действие:** дождаться ответа редакции; до массового сбора
зафиксировать соблюдение `robots.txt`, допустимую частоту запросов, правило
трактовки лицензии каждой статьи и статус контрольной точки.

**Запасной вариант:** `F01_MATHNET` для метаданных; статьи по лазерной физике с
явной открытой лицензией из `F02_OPEN_LICENSE_ARTICLES`.

## 4. Отправленные обращения

Автор проекта подтвердил отправку всех девяти запланированных обращений
31 августа 2026 года. Ниже сохранена сводка от 7 сентября 2026 года;
сведения об ОИЯИ дополнены 17 сентября. Остальные обращения этим обновлением
не перепроверялись.

1. редакция «Успехов физических наук» — ответ ожидается;
2. редакция «Писем в ЖЭТФ» — ответ ожидается;
3. администрация портала sciencejournals.ru — ответ ожидается;
4. редакция «Журнала экспериментальной и теоретической физики» — ответ ожидается;
5. издатель журналов ФТИ им. А. Ф. Иоффе — ответ ожидается;
6. редакция журнала «Квантовая электроника» — ответ ожидается;
7. администрация Math-Net.Ru / МИАН — предложены текстовые подложки и метаданные
   восьми математических журналов через API для создания модели; передача полных
   текстов физических журналов невозможна по действующим договорам; ответ проекта
   отправлен 07.09.2026, ожидаются уточнения о взаимодействии, API и перечне
   физических журналов;
8. администрация КиберЛенинки — ответ ожидается;
9. репозиторий ОИЯИ — статус обновлён 17.09.2026: согласовано получение
   метаданных и доступных полных текстов через DSpace REST API с паузами;
   благодарность отправлена. Условия внутреннего обучения оцениваются отдельно
   по DEC-012; согласование API не является разрешением на публичный выпуск.

Адресаты и копии сохранены в локальной записи
[`evidence/PERMISSION_REQUESTS_SENT_2026-08-31.md`](evidence/PERMISSION_REQUESTS_SENT_2026-08-31.md),
которая не передаётся в Git. SHA-256 записи:
`e00847a85b40db0571f0833a18b7a906580f91150e1266170c163fd66e3e3c06`.
Отправка запроса не считается разрешением и не меняет существующие статусы
операций. После каждого ответа проводится отдельная проверка и добавляются новые
записи `rights-v1` без изменения прежней истории.

### Ответ репозитория ОИЯИ от 02.09.2026

Ниже сохранён первоначальный ответ. Последующее согласование API и уточнение
об отсутствии аффилиаций в метаданных зафиксированы отдельно 17.09.2026.

Представитель ОИЯИ сообщил следующее:

- `pubrepo.jinr.ru` содержит публикации с 2013 года;
- в репозитории находится более 5 000 русскоязычных записей, но файлы полного
  текста есть примерно у 130 из них;
- лицензия задаётся для конкретной публикации; отдельной коллекции CC BY нет;
- фильтр по типу доступа позволяет отделить открытые записи с файлами от записей
  только с метаданными, но сам статус открытого доступа не заменяет проверку вида
  лицензии;
- ОИЯИ готово начать с CSV-выгрузки русскоязычных метаданных и обсуждает варианты
  API или OAI-PMH;
- автоматическое получение файлов полного текста сейчас не предлагается;
  немногочисленные открытые файлы можно скачать вручную;
- препринт ОИЯИ определяется по значению «Объединенный институт ядерных
  исследований» в `dc.publisher` либо по локальному номеру в
  `local.identifier.JINR-preprint`;
- авторов, аффилиацию ОИЯИ и тип лицензии необходимо переносить без искажений;
- ответы о допустимости ML-обучения и публикации весов модели ещё согласуются с
  коллегами ОИЯИ.

Фильтр русскоязычных записей с файлами полного текста:
[открыть в репозитории ОИЯИ](https://pubrepo.jinr.ru/search?f.Lang=ru,equals&spc.page=1&f.has_content_in_original_bundle=true,equals).

**Ответ проекта от 03.09.2026:** предложение о CSV принято, письмо с запросом
всех русскоязычных публикаций, включая записи без полного текста, отправлено.
Запрошены библиографические сведения, внутренние идентификаторы, аффилиации,
аннотации, ключевые слова, тип доступа, лицензия, ссылки на запись и полный текст,
а также `dc.publisher` и `local.identifier.JINR-preprint`.

**Ручное получение от 03.09.2026:** выполнен проход по указанному выше фильтру.
В промежутке с 20:06:03 до 20:27:14 по московскому времени вручную получены 97
PDF общим объёмом 165 571 010 байт. Все файлы имеют сигнатуру PDF, открываются
PyMuPDF и не зашифрованы. Всего в них 823 страницы. У 96 файлов встроенный
текстовый слой пригоден для прямого извлечения. Файл `349-698-1-SM.pdf` визуально читаем: все
10 страниц корректно отображаются. Повреждён именно его встроенный текстовый слой, а
PyMuPDF открывает PDF с восстановлением структуры. Повторная загрузка не требуется; для
получения машинного текста нужно применить OCR и вручную проверить результат.

Контрольные суммы образуют 96 уникальных значений SHA-256. Два файла совпадают
побайтно, но были получены по разным UUID файлов `bitstream`; обе копии сохранены
до сопоставления с карточками публикаций. Количество полученных PDF нельзя пока
считать количеством уникальных статей. Разница между ориентиром «около 130
записей» и 97 фактически полученными файлами также остаётся неразрешённым
пропуском до получения метаданных и сопоставления записей.

Предварительная проверка типа документа отделила 25 материалов, которые не
являются научными статьями: 12 свидетельств о регистрации программ для ЭВМ, два
свидетельства о регистрации баз данных, 10 патентов на изобретения и один патент
на полезную модель. Остаются 72 файла-кандидата, или 71 уникальный кандидат после
учёта совпадающей пары. Это технический предварительный результат, который ещё
нужно сверить с типами публикаций из метаданных репозитория.

Исходные файлы скопированы без изменения байтов в локальный каталог
`data/raw/pdf/jinr_manual_20260903/` под именами UUID `bitstream`. Точные URL из
метаданных загрузки, время, исходные имена, размеры, SHA-256, число страниц и
связь совпадающих файлов сохранены в локальном инвентаре
`manifests/imports/jinr_download_inventory_20260903.jsonl`. Оба пути исключены
из Git. Способ получения зафиксирован как `manual_download`, масштаб — как
`bulk`, поскольку целью прохода был весь результат фильтра, а не заранее
отобранная малая выборка.

Это пока инвентаризация, а не формальная регистрация в рабочих реестрах корпуса.
Для регистрации не хватает соответствия `bitstream` карточке публикации,
проверенных библиографических сведений, лицензии и правообладателя каждой работы,
а также профиля источника, способного различать журналы и препринты внутри
репозитория.

### Согласование API ОИЯИ, зафиксированное 17.09.2026

**ОИЯИ дало согласие на работу с материалами своего репозитория через открытый
DSpace REST API: получение метаданных и доступных полных текстов с паузами
между запросами.** Это новый согласованный способ получения, а не только
предложение вручную скачать файлы или ожидать CSV.

Представитель репозитория направил
[документацию DSpace](https://github.com/DSpace/RestContract/tree/main) и уточнил:

- файлы полных текстов можно получать через API; файлы препринтов постепенно
  добавляются в репозиторий;
- между запросами нужно выдерживать паузы, чтобы избежать блокировки;
  численная частота запросов в письме не задана;
- аффилиации не хранятся; среди соавторов есть люди, не являющиеся сотрудниками
  ОИЯИ, поэтому принадлежность всех авторов к институту не выводится из записи;
- у каждой публикации есть ссылка на источник с полной информацией.

Автор проекта подтвердил отправку благодарности. Текст входящего письма и
подтверждение отправки ответа сохранены локально в
[`evidence/JINR_API_AGREEMENT_RECORDED_2026-09-17.md`](evidence/JINR_API_AGREEMENT_RECORDED_2026-09-17.md);
каталог исключён из Git. 17.09.2026 — дата фиксации в проекте, а не установленная
дата получения входящего письма.
SHA-256 локальной записи:
`c2b39d7e8f2fa5fef787fee843f763fe51c2e10796c047766370876c1c560a83`.

**Текущее решение проекта:** перейти к получению через API, сначала проверить
доступ на одиночных запросах и сопоставить уже имеющиеся PDF с карточками
публикаций. Ожидание именно CSV больше не является обязательным условием.
Исходная опись 97 файлов сохраняет способ получения `manual_download`;
будущие получения через API должны учитываться отдельными событиями.

Первая проверка базового адреса API из старой описи завершилась HTTP 403,
а диагностический запрос — тайм-аутом соединения. Данные через API в рамках
этой проверки ещё не получены; массовая загрузка не запускалась. Базовый адрес,
порядок сопоставления и план проверки описаны в [`JINR_API.md`](JINR_API.md).

В последующей технической проверке ни API, ни пользовательский сайт не приняли
соединение, в том числе при явном выборе IPv4; контрольный HTTPS-запрос к
документации GitHub успешен. Автор проекта разрешил реализацию клиента и выбрал
паузу 60 секунд. Клиент добавлен; его одиночный запрос завершился
`Connection refused`. Настройки VPN и сети не изменялись, успешных загрузок
через новый клиент на тот момент не было. Эти технические отказы не означают отзыва
согласования ОИЯИ.

**Техническое уточнение от 21.09.2026:** корневой ответ HTTP 200 и версия
DSpace 9.1 подтверждены сохранённым HTTP-свидетельством. Успешно получены
конфигурация поиска и первая страница с фильтром `Lang=ru`. В ней указаны
5 380 записей, 54 страницы по 100 записей. После исправления обработки
фактического поля `searchResult` запущена выгрузка всех русскоязычных
метаданных с паузой 60 секунд, без фильтра по году или наличию полного текста.
Проход завершён: проверены 54 страницы и 5 380 уникальных UUID без повторов,
наличие метаданных и языка `ru`, SHA-256 ответов и HTTP-свидетельства.
Регистрация работ и получение PDF этой командой не выполнялись.
Затем по локальному снимку выполнено сопоставление 97 ручных PDF:
85 строк подтверждены по идентификаторам и содержимому, 12 предварительные.
Серверные связи файлов с карточками пока не подтверждены: новая попытка
получить описание файла завершилась `Connection refused` без HTTP-ответа.
Пилот извлечения на трёх PDF выявил ограничение двухколоночного OCR;
результаты и следующие действия — в [`JINR_PILOT.md`](JINR_PILOT.md).

Письмо не устанавливает единую лицензию всех размещённых публикаций и не
содержит отдельного разрешения на публикацию корпуса или весов. Для внутреннего
исследования применяется DEC-012 после проверки условий конкретных работ;
отсутствие отдельной фразы про ML само по себе не создаёт запрет. Согласование
получения не переносится автоматически на `ml_training` и `checkpoint_release`
как статус `allowed`. Рабочие реестры прав этим обновлением документации
не изменяются; регистрация и допуск имеющихся файлов ещё не выполнены.

### Ответ Math-Net.Ru / МИАН, зафиксированный 07.09.2026

**Предыдущая переписка:** после обращения от 31.08.2026 Math-Net.Ru прислал
уточняющие вопросы; подробный ответ проекта о требуемом массиве, результате,
сроках и платформе был отправлен. Этот обмен ранее зафиксирован в журнале
только резюме; отдельные полные тексты писем в `docs/evidence/` не сохранены.

**Новое входящее письмо:** представитель МИАН Дмитрий предложил предоставить
через API текстовые подложки полных текстов и все метаданные восьми журналов,
издателем которых является МИАН:

1. «Дискретная математика»;
2. «Известия Российской академии наук. Серия математическая»;
3. «Математические заметки»;
4. «Математический сборник»;
5. «Теоретическая и математическая физика»;
6. «Теория вероятностей и ее применения»;
7. «Успехи математических наук»;
8. «Функциональный анализ и его приложения».

Предложение прямо относится к созданию модели для математических публикаций.
МИАН заинтересован в результатах её применения к математическим текстам;
формат совместной работы пока не согласован. Доступ к API, документация и
выгрузка в проекте ещё не получены.

**Физические журналы:** МИАН не является их издателем и не может передавать
полные тексты третьим лицам по действующим договорам. Для получения текстовых
подложек или PDF нужно разрешение соответствующего журнала либо прямая
передача данных самим журналом. Перечень таких физических журналов в ответе
не указан. Представитель также предупредил, что часть физических PDF требует
OCR из-за отсутствия читаемой текстовой подложки.

**Способ получения:** зафиксирована прямая просьба воздержаться от автоматического
скачивания полных текстов с сайта Math-Net.Ru. Предлагаемый API математических
журналов — отдельный канал; предложение не разрешает автоматическое скачивание
PDF с сайта.

**Доказательство:** полный переданный текст письма с нормализованными пробелами
и переносами сохранён в локальной записи
[`evidence/MATHNET_RESPONSE_RECORDED_2026-09-07.md`](evidence/MATHNET_RESPONSE_RECORDED_2026-09-07.md).
SHA-256 записи:
`0b1ad1bea8ae755f4b14fd4196df65ef6d01eb141f2ec9e54b64bf107b816908`.
7 сентября — дата фиксации; дата самого письма и почтовые заголовки не
предоставлены. Полная переписка остаётся вне Git согласно правилам проекта.

**Ответ проекта от 07.09.2026:** письмо о заинтересованности в математических
данных отправлено; автор проекта подтвердил отправку в рабочей сессии.
Запрошены формат взаимодействия, порядок получения доступа к API и документации,
а также перечень физических журналов для обращения в ещё не охваченные редакции.
Текст ответа и подтверждение отправки сохранены в отдельной локальной записи
[`evidence/MATHNET_REPLY_SENT_2026-09-07.md`](evidence/MATHNET_REPLY_SENT_2026-09-07.md).
SHA-256 записи:
`a868802fa9048f95fd57a5b9e2826b0729fd4d66b203097928b09b8a8826fe09`.

**Следующее действие:** ожидать ответ МИАН по этим вопросам. Доступ к API и
выгрузка пока не получены. Предложение по математике учитывается как
дополнительное направление; состав физического corpus v1 и его обязательные
эксперименты H1/H2 этим письмом не изменены.

## 5. Запасные варианты

### F01_MATHNET — Math-Net.Ru

Роль: библиографические записи и контакт с МИАН для получения через API
текстовых подложек и метаданных восьми математических журналов для создания
модели. Предложение зафиксировано 07.09.2026; это дополнительное направление,
а не готовый источник полных текстов физических журналов.

МИАН не может передавать физические полные тексты по действующим договорам:
за разрешениями или прямой передачей данных следует обращаться к самим
журналам. Автоматическое скачивание полных текстов с сайта Math-Net.Ru не
выполнять согласно прямой просьбе представителя. Доступ к предложенному API
оформляется отдельно; наличие копии на платформе не переносит права на
распространение автоматически.

### F02_OPEN_LICENSE_ARTICLES — статьи с явной открытой лицензией

Роль: замена работ, для которых нет правомерного доступа, совместимого способа
получения или отсутствует возможность нужного публичного выпуска. Включение
выполняется по статье, а не по домену: сохраняются вид лицензии, URL лицензии,
версия, право на производные материалы и совместимость с выпуском модели. В
первую очередь проверяются репозитории авторов, институциональные архивы и
журнальные страницы с явной лицензией.

### F03_BIBLIOGRAPHIC_METADATA — ВИНИТИ, eLIBRARY.RU или иной каталог

Роль: подтверждение кодов ГРНТИ и библиографических связей для H2 без
автоматического переноса полного текста. Доступность записи, право на массовое
получение метаданных и право использовать их как метки проверяются отдельно.

### F04_JINR_REPOSITORY — репозиторий ОИЯИ

Роль: источник русскоязычных метаданных и доступных полных текстов, включая
собственные препринты ОИЯИ. Согласованный маршрут — DSpace REST API с паузами
между запросами; сведения о доступе и первичной проверке приведены в
[`JINR_API.md`](JINR_API.md). Файлы препринтов пополняются; оценки объёма из
письма 02.09.2026 не являются пределом сбора.

3 сентября вручную получены и инвентаризированы 97 PDF. 21 сентября выполнено
локальное сопоставление с метаданными публикаций: 85 сопоставлений подтверждены
по идентификаторам и содержимому, 12 остаются предварительными. Серверные связи
`bitstream → item` ещё не подтверждены; работы не зарегистрированы в рабочих
реестрах. Подробности — в [`JINR_PILOT.md`](JINR_PILOT.md).
Принадлежность к репозиторию или открытый доступ не считаются автоматически
лицензией CC BY; лицензия, издатель, правообладатель и тип работы проверяются
на уровне публикации. Наличие работы в репозитории не означает, что все её
авторы — сотрудники ОИЯИ; аффилиации репозиторий не хранит.

### Предварительный результат по ВИНИТИ РАН

[База данных ВИНИТИ РАН](https://www.viniti.ru/database) платная и требует
регистрации; её официальное описание подтверждает подокументную индексацию
кодами ГРНТИ, УДК и рубрикатора ВИНИТИ. [Электронный
каталог](https://www.viniti.ru/serviscatalog) открыт, хотя часть возможностей
требует регистрации. В карточках могут присутствовать коды ГРНТИ, русский
перевод заголовка, URL или DOI и авторская аннотация, но аннотация и ключевые
слова доступны не для каждой работы.

Официальные страницы не описывают поле основной рубрики и не разрешают считать
первый код в списке основным. Поэтому до письменного разъяснения ВИНИТИ или
получения выгрузки с отдельным признаком основной рубрики действуют правила:

- сохранять полный список найденных кодов как исходные сигналы;
- для каждого кода ставить `is_primary_explicit=false`;
- не использовать порядок кодов для формирования `h2_labels`;
- не присваивать такому сигналу происхождение `bibliographic_grnti` основной
  метки.

Публичная документация API, открытая лицензия на метаданные и правила массового
автоматизированного получения не найдены. Ручная калибровочная проверка 30 работ
может использовать каталог как вторичный источник, но автоматическая выгрузка и
построение основного набора на метаданных ВИНИТИ требуют согласования способа,
формата, объёма и права хранения с информационной службой ВИНИТИ.

### Проверка институционального доступа к eLIBRARY.RU

Публичная часть проверки выполнена 27 августа 2026 года. Наличие связи
организации с РИНЦ или SCIENCE INDEX не считается доказательством подписки на
полные тексты: эти сервисы и доступ к подписным журналам проверяются отдельно.

#### НИЯУ МИФИ — страница библиотеки указывает доступ, подписку нужно проверить

[Научная библиотека НИЯУ МИФИ](https://library.mephi.ru/english/resources/1468/1483)
указывает доступ к 124 журналам eLIBRARY.RU по атомно-энергетической тематике.
На странице нет даты обновления и полного перечня доступных лет, поэтому она
свидетельствует о наличии такого институционального предложения в каталоге
библиотеки, но не подтверждает действующую на 27 августа 2026 года подписку.

[Руководство пользователя eLIBRARY.RU](https://elibrary.ru/projects/subscription/manual_elibrary_for_user.pdf)
описывает проверку по зарегистрированному IP организации: на главной странице
появляется окно о подписке, а ссылка «Список журналов» показывает доступные
издания и периоды. Для полного текста нужен личный вход в eLIBRARY.RU. Интерфейс
позволяет сохранить или распечатать доступную отдельную статью.

Для ресурсов на странице МИФИ указаны доступ из сети университета и запрет на
копирование целых томов и выпусков, материалов конференций, всех результатов
поиска и использование автоматических загрузчиков. Поэтому статусы на текущем
этапе такие:

- ручное получение отдельной статьи — `conditional`: статья должна входить в
  фактически действующую подписку и доступный период, пользователь должен быть
  авторизован, а использование остаётся закрытым и некоммерческим;
- закрытое хранение, оценка и обучение модели на правомерно полученных отдельных
  статьях — `conditional` по `lawful_access_research_assumption`;
- автоматическое или систематическое получение выборки и массовый экспорт
  метаданных — `permission_required`: руководство eLIBRARY.RU требует
  предварительного письменного разрешения на значительное, систематическое или
  роботизированное копирование;
- передача и публикация корпуса полных текстов — `permission_required`:
  требуется отдельное письменное разрешение, тогда как статус метаданных,
  коротких примеров и агрегированных результатов проверяется независимо.

[VPN НИЯУ МИФИ](https://it.mephi.ru/vpn) даёт удалённое подключение к внутренней
сети, но его инструкция не подтверждает, что обращение к eLIBRARY.RU выходит
через зарегистрированный адрес подписчика. Работу подписки через VPN нужно
проверить практически. Вопросы о базах данных следует направлять научной
библиотеке по адресу `library@mephi.ru` либо указанным на странице ресурсов
ответственным сотрудникам.

#### ФГУП «ВНИИА им. Н. Л. Духова» — доступ не подтверждён

На публичном сайте ВНИИА и в открытых документах не найдено подтверждение
действующего договора на полнотекстовые коллекции eLIBRARY.RU. Наличие карточки
организации и публикаций её сотрудников в РИНЦ ничего не говорит о подписке.
До проверки из внутренней сети или письменного ответа все операции с подписными
полными текстами имеют статус `unknown` и не используются для корпуса.

[Официальные контакты ВНИИА](https://www.vniia.ru/coordinates/koordinaty.php):
`vniia@vniia.ru`, +7 (499) 978-78-03; канцелярия — `mailbox79@vniia.ru`,
+7 (499) 972-84-99, добавочный 14-30. У канцелярии нужно запросить контакт
сотрудника, отвечающего за научно-техническую библиотеку, научно-техническую
информацию или электронные ресурсы. Канцелярия сама не считается подтверждённой
службой библиотеки.

#### Оставшаяся практическая проверка

Для каждой организации из её внутренней сети, а затем через удалённый доступ,
если он предоставляется организацией, нужно:

1. сохранить датированное подтверждение того, какую организацию определил
   eLIBRARY.RU;
2. проверить наличие окна о подписке и сохранить перечень журналов с доступными
   периодами;
3. после личного входа вручную открыть и сохранить одну подписную статью, не
   относящуюся к открытому доступу;
4. установить охваченные договором площадки и IP-адреса, способ удалённого
   доступа и срок действия подписки;
5. отдельно получить письменный ответ о допустимости получения всего доступного массива,
   автоматического получения, экспорта метаданных и закрытого обучения модели.

До этой проверки возможность открыть отдельный PDF не считается разрешением на
массовую загрузку. Для систематического получения eLIBRARY.RU остаётся запасным,
а не основным источником корпуса.

## 6. Уточнение автоматического получения и публичного выпуска

Письменный ответ правообладателя не является обязательным условием внутреннего
обучения, если правомерный доступ подтверждён, применимые условия проверены и в
них нет явного несовместимого запрета. Обращение требуется, когда источник прямо
требует разрешение, договор или технические правила неоднозначны для планового
способа и масштаба получения либо проект намерен публично выпускать артефакт с
неустановленным статусом.

В обращении задаются только относящиеся к такой неясности вопросы:

1. Можно ли автоматически получить все доступные русскоязычные статьи по физике,
   каким способом и с какой частотой запросов?
2. Есть ли ограничения на объём закрытого исследовательского хранения,
   длительность хранения, атрибуцию, удаление по запросу или круг участников?
3. Какие метаданные, агрегированные показатели, короткие примеры,
   идентификаторы и производные наборы можно публиковать?
4. Можно ли опубликовать полученную контрольную точку модели и токенизатор без
   публикации и восстановления исходных текстов?

Назначение закрытого некоммерческого корпуса и дополнительного предварительного
обучения можно описать как контекст запроса, но отсутствие отдельного
положительного ответа про ML не отменяет рабочее допущение проекта. Полученный
ответ сохраняется как доказательство с датой, отправителем, областью действия и
контрольной суммой файла и имеет приоритет над допущением.

## 7. Критерий завершения правового аудита

Для внутреннего использования источника должны быть зафиксированы правомерное
основание доступа, проверенные применимые условия, способ и масштаб получения,
а также отсутствие явного запрета на нужную операцию. В этом случае закрытые
некоммерческие хранение, оценка и ML-обучение получают `conditional` на основании
`basis_type=lawful_access_research_assumption`; отдельная фраза про ML в условиях
источника не требуется.

Для запуска этапа 3 нужно подтвердить как минимум четыре `source_group_id`, для которых
выполнены условия
`acquisition_allowed`, `storage_allowed`, `evaluation_allowed` и
`ml_training_allowed`. `permission_required` и `prohibited`, а также
`terms_review_pending` с непрочитанным применимым договором блокируют только
затронутые операции. Неизвестный статус перераспространения, производного набора
или контрольной точки не блокирует закрытый внутренний эксперимент, но такой
артефакт нельзя публиковать до отдельного решения.

После достижения минимального порога H1 в 2 000 работ сбор не останавливается: до момента
`corpus_cutoff_at` получаются все допущенные работы из всех согласованных источников.

Если это невозможно, меняется состав источников или оформляется новое решение
о сокращении обязательной части проекта до метаданных и инженерного пилота.
Порог нельзя обходить только потому, что файл технически скачивается: доступ не
должен получаться через обход авторизации, платного режима, явного запрета или
ограничения массового копирования.
